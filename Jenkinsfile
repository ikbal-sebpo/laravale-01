pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Jenkinsfile successfully loaded!'
                sh 'php -v'
            }
        }
    }
}

